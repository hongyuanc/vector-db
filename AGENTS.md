this project is a educational project, for me to grasps concepts and implemenatation along the way.

when making updates/implementations, do it in small chunks (one commit sized chunks), explaining what was there before, what was implemented (most importantly why we implement it), and the next steps.

also i want this project to be summed in a technical description document MD file, something we add on and build along the way, so when the project is complete, i would have a technical documentation i can review (for interviews etc.) when i come back to this project.

for git operations, agents should not run integration or remote-sync commands such as `git merge`, `git pull`, `git push`, `git rebase`, or pull-request creation. i will handle those manually because they are sometimes inconsistent in this environment. agents may still use local inspection commands, `git add`, and `git commit` when a change is ready.

please dont use emojis in documentation.
